import Cards from '../Cards/Cards';
import CardHolder from '../Cards/CardHolder';
function Home(){
  return (
    <section>
      <CardHolder>
        <Cards>
        </Cards>
      </CardHolder>
    </section>
  );
}

export default Home;
