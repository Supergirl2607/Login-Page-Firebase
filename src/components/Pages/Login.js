import Login from '../Login/Login';
import CardHolder from '../Cards/CardHolder';
function LoginPage(){
  return (
    <CardHolder>
    <section>
      <Login></Login>
    </section>
    </CardHolder>
  )
}

export default LoginPage;
